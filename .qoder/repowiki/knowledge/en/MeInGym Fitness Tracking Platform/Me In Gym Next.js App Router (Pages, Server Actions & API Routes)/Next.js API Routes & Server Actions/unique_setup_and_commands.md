Image upload endpoints write directly to `public/uploads` under the process working directory and attempt to `chown` files to the current uid/gid with mode `0o775`; this requires the runtime user to have permission to change ownership (relevant in Docker deployments). Mobile auth endpoints expect a `Bearer <token>` Authorization header and rely on Redis-backed Bull queues for job processing.